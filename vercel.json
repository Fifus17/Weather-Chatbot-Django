{
    "builds": [{
        "src": "Backend/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "20mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "Backend/wsgi.py"
        }
    ]
}