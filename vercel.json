{
    "builds": [{
        "src": "Backend/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "2gb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "Backend/wsgi.py"
        }
    ]
}